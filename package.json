{
  "name": "arc-transaction-observer",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only Arc Testnet RPC, gas, block and USDC accounting observer",
  "type": "module",
  "scripts": {
    "health": "tsx src/health.ts",
    "receipt": "tsx src/receipt.ts",
    "build": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "tsx": "^4.20.5",
    "typescript": "^6.0.2"
  }
}
